# 公开发布清单

这份清单用于把本地发布候选变成可公开、可复核的 GitHub 项目。任一必选项未完成，都只保留“发布候选”状态。

当前状态（2026-08-15）：GitHub 初始版本为 v0.1，对应内部验收包 V7.5.15；本地发布候选门禁已完成。未勾选项仍是公开发布硬门槛，不得用本地通过替代。

## 1. 法律与联系渠道

- [ ] 版权人选择并加入正式 `LICENSE`；许可证名称与 README 一致。
- [ ] 确认仓库没有来源不明、无权再分发的代码、图片、文档或凭据。
- [ ] 在仓库 Security 设置中启用私密漏洞报告；若平台无法启用，先在 `SECURITY.md` 加入可用的私密联系渠道。

## 2. 发布内容

- [x] `skill/` 与权威 Skill 目录逐文件一致。
- [x] `dist/` 只包含当前版本 ZIP、`SHA256SUMS`、`FILES.sha256` 和独立验证器。
- [x] 独立验证器的 SHA-256 等于 `FILES.sha256` 中 `./scripts/verify_release_package.py` 的值。
- [x] 隐私扫描没有真实姓名、个人绝对路径、私人文档链接、API key、token、二维码、会话、日志或账号 ID。
- [x] README 的能力、平台支持和未验证范围与本轮证据一致。

## 3. 发布门禁

- [ ] macOS 与 Linux CI 全部通过。
- [x] CI 中的第三方 Actions 固定到其官方仓库 v7.0.0 标签对应的完整提交 SHA，而不是可移动标签。
- [x] 使用本次声明支持的干净 Hermes、MCP Python 与真实 Node.js 绝对路径运行完整 `validate_skill.py --hermes ... --mcp-python ... --node ...`，且无 skip。
- [x] 使用具备 PyYAML 的环境运行官方 `quick_validate.py`。
- [x] 在全新私有目录核验 ZIP 哈希、验证器哈希、逐文件哈希，并无覆盖解压。
- [x] 从解压副本再次运行完整验证，结果与权威目录一致。

## 4. GitHub 发布

- [ ] 首次推送前保持仓库私有，确认默认分支保护、只读 Actions 权限和私密漏洞报告。
- [ ] CI 通过后创建不可变版本标签 `v0.1`；标签必须与根目录 `VERSION` 一致。
- [ ] GitHub Release 上传四个制品，不把本地临时目录或 `skill/` 的第二份手工 ZIP 当成发布物。
- [ ] Release 页面记录 ZIP 的 SHA-256、实际验证平台和明确未验证项。
- [ ] 从公开 Release 重新下载四个制品，再按接收者流程完整安装一次；本地构建产物不能替代公开下载证据。

## 5. 真实能力边界

- [x] 基础微信助手的真实验收记录与增强能力记录分开。
- [ ] 飞书分别记录工具层读取、模型层问答、微信层结果，以及只读或可写状态。
- [ ] 编程分别记录真实 Codex 准备、二次确认应用、相关测试、回滚和微信回执。
- [x] 日常自动化分别记录创建回执、手动运行、持久运行记录、微信送达、暂停、恢复和移除。

当前已记录：本地知识库真实模型层读取、创建和回滚成功；V7.5.15 还从“没有知识库”起点复验新空目录初始化、重复初始化拒绝、读取、搜索、创建、更新、并发版本拒绝、两级回滚和逐字节恢复，但微信入站尚未闭环。飞书固定文档真实读取、越界链接联网前拒绝、最小创建权限授权，以及锁定个人文档库的无私人内容文档真实创建和读回均成功；正文、链接、文档 ID 和密钥没有输出，隔离验收助手使用固定读取与固定父目录创建的 2 工具 MCP，飞书微信层仍未冒充通过。Codex 已使用不覆盖用户全局 DeepSeek 配置的专用官方登录目录完成工具层准备、应用、测试和回滚；V7.5.15 用实际保存、显式绑定 Node.js 的 Codex MCP 再次完成 3 次确认、真实准备、应用、测试、回滚、逐字节恢复和 Git 干净，并以故障注入证明任务记录写盘失败时项目自动恢复到与旧记录一致；仍需微信原界面的逐次授权回执。Cron 已取得创建、手动运行、`completed`、iLink 发送、暂停、恢复、删除和零残留证据；V7.5.10 真机截图确认成功消息只有中文正文，没有 `Cronjob Response` 英文外层，完整微信闭环通过。V7.5.11 另补齐 `/new` 英文确认卡片的中文解释与重启失效恢复规则。

本地测试、模拟服务、CI、模型调用和命令成功都不能单独证明公开下载、真实微信送达或开机恢复。
